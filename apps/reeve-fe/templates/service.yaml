apiVersion: v1
kind: Service
metadata:
  name: reeve-fe-service
spec:
  type: ClusterIP
  selector:
    app: community
    tier: frontend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
